require 'minitest/autorun'
require './lib/hill_climb.rb'

describe HillClimb do
  context "when provided invalid arguments" do
    
  end
  
  context "when provided valid arguments" do
    
  end
end